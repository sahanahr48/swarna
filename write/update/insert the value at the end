#include<stdio.h>
#define CAP 100
int main()
{
    int a[CAP]={1,2,3,4};
    int n=4;
    int x=45;
    if(n<CAP)
    {
        a[n]=x;
        n++;
    }
    else
    {
        printf("array is full");
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",a[i]);
}
}
